rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ログイン中の家族アカウント(uid)のデータだけ読み書きできる
    match /families/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
